#!/bin/bash
# Install core packages on a fresh machine
set -euo pipefail

{{ if eq .chezmoi.os "darwin" -}}
# ── macOS: Homebrew ──────────────────────────────────────────────────

if ! command -v brew &>/dev/null; then
  echo "Installing Homebrew..."
  /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
  echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> "$HOME/.zprofile"
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi

# The full package set is the single source of truth in ~/.config/Brewfile
# (+ Brewfile.fonts). It's installed by run_onchange_after_02-brew-bundle, which
# re-runs automatically whenever either Brewfile changes.
echo "Homebrew ready; packages are applied by the Brewfile bundle step."

{{ else if eq .chezmoi.os "linux" -}}
# ── Linux: base shell + mise bootstrap ───────────────────────────────
# The full CLI/zen kit (eza, bat, fzf, yazi, lazygit, starship, atuin, …) is
# installed by mise — run_onchange_after_03-mise-install — identically to macOS.
# Here we only need the base shell, git, and btop (btop ships no macOS prebuilt,
# so it's brew on mac and distro-installed here on Linux).
SUDO=""
if [ "$(id -u)" -ne 0 ] && command -v sudo &>/dev/null; then
  SUDO="sudo"
fi

if command -v apt-get &>/dev/null; then
  $SUDO apt-get update -qq
  $SUDO apt-get install -y -qq btop curl gh git unzip xclip zsh
elif command -v dnf &>/dev/null; then
  $SUDO dnf install -y btop curl gh git unzip zsh
elif command -v pacman &>/dev/null; then
  $SUDO pacman -Syu --noconfirm btop curl gh git unzip zsh
else
  echo "No supported package manager (apt/dnf/pacman); assuming the image provides these tools."
fi

# mise: the cross-platform tool manager. Everything else flows from it.
if ! command -v mise &>/dev/null; then
  curl https://mise.run | sh
fi

echo "Linux base ready; run_onchange_after_03-mise-install provisions the tool kit."
{{ end -}}
